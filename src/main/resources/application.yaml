spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jwtdb //databaza adi
    username: postgres
    password: p1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  main:
    allow-circular-references: true

server:
  port: 8080